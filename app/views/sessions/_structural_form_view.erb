<%# FIXME: put is wrong method %>
<%= link_to 'Open Thermal Paraview', paraview_thermal_path(session.thermal), method: :put, class: 'btn btn-default btn-sm', target: "_blank" %>

<hr>

<%= bootstrap_form_tag url: submit_structural_path(session.thermal.structural), method: 'put' do |f| %>
  <%= f.number_field :hours, value: 8, in: 1..3600 %>
  <%= f.submit 'Submit WARP3D', class: "btn btn-success btn-sm" %>
<% end %>
