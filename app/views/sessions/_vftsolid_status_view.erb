<%# FIXME: this button should not do anything! Just a label! %>
<% if session.conn_avail? %>
  <%= link_to "Open VFTSolid", session.conn_view.render(:awesim), class: 'btn btn-primary btn-sm' %>
<% else %>
  <button class="btn btn-primary btn-sm" disabled="disabled">VFTSolid: <%= session.status_str %></button>
<% end %>
