(function() {
  <% @sessions.each do |session| %>
    var cached_hash, row_id;
    row_id = "#session_row_<%= session.id %>";
    cached_hash = $(row_id).data('hash');
    if (cached_hash != "<%= session.hash %>") {
      <%= render "shared/update_row", session: session %>
    }
  <% end %>
})();
