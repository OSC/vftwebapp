<%= link_to 'Stop', stop_thermal_path(session.thermal), method: :put, class: 'btn btn-danger btn-sm', data: { confirm: 'Are you sure?' }, remote: true if session.subject.active? %>
<%= session.msg if session.msg %>
