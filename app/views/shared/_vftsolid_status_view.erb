<%# FIXME: this button should not do anything! Just a label! %>
<% if session.conn_avail? %>
  <p>
    <%= link_to "Open VFTSolid", session.conn_view.render(:awesim), class: 'btn btn-default btn-sm btn-fixwidth' %>
  </p>
<% end %>

<%= session.msg if session.msg %>

<%= link_to 'Stop', stop_session_path(session), method: :put, class: 'btn btn-danger btn-sm btn-fixwidth', data: { confirm: 'Are you sure?' }, remote: true if session.active? %>
