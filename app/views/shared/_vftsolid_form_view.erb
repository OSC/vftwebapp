<p>
  <%= link_to 'Launch VFTSolid', submit_session_path(session), method: :put, class: 'btn btn-success btn-sm launch-vftsolid launch-btn btn-fixwidth', remote: true %>
</p>
<%= link_to icon('step-forward', 'Skip Stage'), submit_session_path(session, {validate: true}), method: :put, class: 'btn btn-default btn-sm launch-btn btn-fixwidth', remote: true %>
