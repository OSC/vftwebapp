<%= session.msg if session.msg %>
<p>
  <%= link_to 'Stop', stop_structural_path(session.thermal.structural), method: :put, class: 'btn btn-danger btn-sm btn-fixwidth', data: { confirm: 'Are you sure?' }, remote: true if session.subject.active? %>
</p>
<%= link_to 'Open Thermal Paraview', paraview_thermal_path(session.thermal), method: :put, id: "thermal_paraview_#{session.id}", class: 'btn btn-thermal-results btn-sm paraview-popover btn-fixwidth', remote: true %>
